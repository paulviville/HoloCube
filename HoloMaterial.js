import * as THREE from "./three/three.module.js";

const vertexShader = `

`;

const fragmentShader = `

`;

class HoloMaterial extends THREE.RawShaderMaterial {
	constructor ( colorTexture, depthTexture ) {
		console.log( `HoloMaterial - constructor` ); 
		
	}


}